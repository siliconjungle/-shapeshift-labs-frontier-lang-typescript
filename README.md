# @shapeshift-labs/frontier-lang-typescript

TypeScript projection adapter for Frontier Lang semantic source documents.

## Install

```sh
npm install @shapeshift-labs/frontier-lang-typescript
```

This package depends on `@shapeshift-labs/frontier-lang-kernel` and owns TypeScript/JavaScript projection concerns. The kernel stays target-neutral.

## License

MIT.
